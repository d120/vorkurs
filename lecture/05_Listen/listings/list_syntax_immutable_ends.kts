val myList = listOf(42, 56, 1, 2, 5)

// Erstes element lesen
val erstesElement = myList[0]
val erstesElement = myList.first()

// Letztes element lesen
val erstesElement = myList[myList.size - 1]
val letztesElement = myList.last()
